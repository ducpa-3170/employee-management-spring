spring:
  profiles:
    default: dev

# JWT Configuration
jwt:
  secret: YourVerySecureSecretKeyForJWTTokenGenerationThatShouldBeAtLeast256BitsLongForHS512Algorithm
  expiration: 86400  # 24 hours in seconds (24 * 60 * 60)
